/Debug/
.import
*.apk
*.import

# Signing keys — must never be committed
*.keystore
*.jks
*.p12

# Android build output and caches (regenerated by Gradle/Godot)
android/build/build/
android/build/.gradle/
android/build/libs/
